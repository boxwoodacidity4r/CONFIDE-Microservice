# Phase 3: CAC 聚类（基于 S_final + EDL 不确定性 U）

本阶段根据 Phase 1 生成的融合相似度矩阵 `S_final` 与 Phase 2 推理得到的不确定性矩阵 `U`，输出两套分区：
- **Baseline**：只用 `S_final`
- **CAC-Final**：用 `S_final` + `U`（惩罚不确定边）

输出分区文件会被 Phase 4 用于对齐 GT 做评估。

---

## 0. 前置条件（必须已有的输入）
对每个 system（acmeair/daytrader/jpetstore/plants）确保已存在：
- `data/processed/fusion/{system}_S_final.npy`（Phase 1）
- `data/processed/edl/{system}_edl_uncertainty.npy`（Phase 2）
- `data/processed/fusion/{system}_class_order.json`（Phase 1，用于后续映射/可视化）
- `data/processed/groundtruth/{system}_ground_truth.json`（默认开启 StrategyA：GT>=0 过滤 universe）

---

## 1. 脚本概览：每个脚本做什么？

### 1.1 `phase3_cac_evaluation.py`（主脚本）
**用途**：对指定系统运行 Louvain 聚类并输出两套分区：
- Baseline（仅 `S_final` 建图）
- CAC-Final（`S_final` + `U` 不确定性惩罚）

**核心能力**：
- StrategyA（默认开启）：按 GT>=0 过滤 universe，避免负标签污染
- DPEP（默认开启）：动态阈值剪枝（percentile + cap），避免不同系统密度差异导致调参不可迁移
- Sigmoid uncertainty decay：`W = S * (1 - sigmoid(alpha*(U-beta)))`
- 可选后处理：合并小簇（避免 singleton/microservice）
- 输出簇大小统计：合并前/后都会打印统计（min/median/p75/max、singletons、top10）
- 参数持久化：写入 `{system}_cac_params.json` 便于复现审计

**典型输出文件**：
- `data/processed/fusion/{system}_baseline_partition.json`
- `data/processed/fusion/{system}_cac-final_partition.json`
- `data/processed/fusion/{system}_cac_params.json`

### 1.2 `run_final_eval.py`（论文级“一键全家福”汇总）
**用途**：按“黄金参数”（可在脚本顶部 `GOLDEN` 字典里改）自动跑四个系统，解析终端输出并生成：
- Paper-ready CSV：`results/ablation/phase3_best_all_systems_YYYYmmdd_HHMMSS.csv`
- Paper-ready Markdown：`results/ablation/phase3_best_all_systems_YYYYmmdd_HHMMSS.md`

> 说明：该脚本会调用 `phase3_cac_evaluation.py`，并从其输出表格中抽取 Baseline/CAC 的 Q、IFN、IFN_Ratio、Services。

### 1.3 `summarize_best_points.py`（轻量级快速汇总）
**用途**：读取每个系统最近一次写入的 `{system}_cac_params.json`，快速打印参数摘要。

---

## 2. 一键运行（推荐）

### 2.1 单系统运行
```powershell
python scripts/multimodal/phase3/phase3_cac_evaluation.py acmeair
```

### 2.2 四系统批量运行
```powershell
python scripts/multimodal/phase3/phase3_cac_evaluation.py acmeair daytrader jpetstore plants
```

脚本会根据内置 `SYSTEMS.range` 自动选择目标服务数范围。

---

## 3. 论文级：一键生成“全家福表格”（强烈推荐）
运行该脚本会自动跑四个系统并生成 CSV+Markdown 汇总表：

```powershell
python scripts/multimodal/phase3/run_final_eval.py
```

输出位置：
- `results/ablation/phase3_best_all_systems_*.csv`
- `results/ablation/phase3_best_all_systems_*.md`

如何修改“黄金参数”：编辑 `scripts/multimodal/phase3/run_final_eval.py` 顶部的 `GOLDEN`：
- system -> (target_min, target_max, dpep_cap)

---

## 4. 论文/对照实验常用参数（手动运行）

### 4.1 固定目标服务数范围（严格对齐 GT 的对照）
以 AcmeAir 固定 5 个服务为例：

```powershell
python scripts/multimodal/phase3/phase3_cac_evaluation.py acmeair --target_min 5 --target_max 5
```

### 4.2 放宽目标范围（探索性/调参）

```powershell
python scripts/multimodal/phase3/phase3_cac_evaluation.py acmeair --target_min 3 --target_max 8
```

### 4.3 指定自定义 U（不确定性矩阵）
单系统：

```powershell
python scripts/multimodal/phase3/phase3_cac_evaluation.py acmeair --u_path data/processed/edl/acmeair_edl_uncertainty_kl5.npy
```

多系统批量（顺序与 systems 一一对应，逗号分隔）:

```powershell
python scripts/multimodal/phase3/phase3_cac_evaluation.py acmeair daytrader --u_path data/processed/edl/acmeair_edl_uncertainty_kl5.npy,data/processed/edl/daytrader_edl_uncertainty_kl5.npy
```

### 4.4 图构建阈值（当图过稀/找不到有效分区时）
默认只保留 `weight > edge_min_weight` 的边。

- 默认开启 DPEP：会动态计算 `tau = min(percentile(nonzero(S)), cap)` 并最终更新 `edge_min_weight`
- 若想关闭 DPEP 并手工控制阈值：使用 `--no_dpep`

示例（手工阈值 + 关闭 DPEP）：

```powershell
python scripts/multimodal/phase3/phase3_cac_evaluation.py jpetstore --no_dpep --edge_min_weight 0.22
```

关闭图诊断输出：

```powershell
python scripts/multimodal/phase3/phase3_cac_evaluation.py acmeair --no_graph_diag
```

---

## 5. 输出文件（Phase 4 会使用）
运行后会写入：
- `data/processed/fusion/{system}_baseline_partition.json`
- `data/processed/fusion/{system}_cac-final_partition.json`
- `data/processed/fusion/{system}_cac_params.json`（记录本次运行策略/参数/簇大小统计，便于复现/审计）

---

## 6. 结果“版本化快照”（强烈推荐，避免被覆盖）
`phase3_cac_evaluation.py` 的分区 JSON 默认会被下一次实验覆盖。

建议在拿到“最优点”后立刻做一次快照备份（示例：JPetStore cap=0.14 的 CAC-Final）：

```powershell
Copy-Item ".\data\processed\fusion\jpetstore_cac-final_partition.json" `
          ".\data\processed\fusion\jpetstore_cac_final_cap0.14_alpha15.json"
```

---

## 7. 可选：把分区映射为类名（便于人工检查）

```powershell
python scripts/multimodal/phase3/map_results_to_classes.py acmeair
```

---

## 8. 可选：重排 U 矩阵热力图（调试/展示）
说明：该图用于辅助观察块结构，**不作为论文必需证据**。

```powershell
python scripts/multimodal/phase3/visualization/draw_reordered_heatmap.py acmeair --u_path data/processed/edl/acmeair_edl_uncertainty.npy --part_path data/processed/fusion/acmeair_cac-final_partition.json --order_path data/processed/fusion/acmeair_class_order.json --out_dir data/processed/visualization --vmax 0.3
```

---

## 9. 常见问题（Troubleshooting）
- **找不到分区 / 服务数不在范围内**：
  - 尝试放宽 `--target_min/--target_max`
  - 调整 `--dpep_cap`（或关闭 DPEP 并调 `--edge_min_weight`）

- **出现 singleton / 小簇过多**（例如 9 个 singleton）：
  - 降低剪枝强度：减小 `edge_min_weight` 或启用/调小 `dpep_cap`
  - 启用后处理合并：`--merge_small_clusters --min_cluster_size 3`
  - 注意：若节点在图中对外没有边（被剪掉），则合并无法发生

- **缺少输入文件**：检查 `data/processed/fusion/` 与 `data/processed/edl/` 下是否存在本 README 第 0 节列出的输入。

---

## Phase 4（评估 / GT 对齐）
评估脚本已迁移到 `scripts/multimodal/phase4/`：
- 评估：`scripts/multimodal/phase4/evaluate_partition_f1.py`
- 边界冲突诊断：`scripts/multimodal/phase4/diagnose_boundaries.py`