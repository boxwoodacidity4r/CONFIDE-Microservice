# Multimodal Microservice Extraction Pipeline

本项目提供对 4 个经典单体基准系统（`acmeair`, `daytrader`, `jpetstore`, `plants`）的多模态特征构建、相似度矩阵生成、聚类与评估脚本。

## 0. 环境准备（Python 依赖 + 可复现运行）

建议使用虚拟环境，并确保 `requirements.txt` 依赖已安装。

```powershell
cd d:\multimodal_microservice_extraction

# (可选) 创建/激活 venv
python -m venv .venv
.\.venv\Scripts\Activate.ps1

pip install -r requirements.txt
```

> 说明：语义 embedding 默认使用 HuggingFace 模型 `microsoft/graphcodebert-base`。
> - 若机器无法联网，需提前把模型下载到本地 HuggingFace cache（或将 cache 目录拷贝到目标机器）。
> - 代码已尽量优先使用本地 cache（offline-friendly）。

## 1. 语义模态（Semantic）：提取 → 向量化（Embedding）

本项目在 `data/processed/semantic/` 生成语义 JSON，在 `data/processed/embedding/` 生成语义向量（`.pt`）。

### 1.1 语义特征提取（生成 semantic JSON）

脚本：`scripts/semantic/run_semantic_extraction.py`

```powershell
cd d:\multimodal_microservice_extraction
python scripts\semantic\run_semantic_extraction.py
```

输出（每个系统 1 个）：

- `data/processed/semantic/acmeair_semantic.json`
- `data/processed/semantic/daytrader_semantic.json`
- `data/processed/semantic/jpetstore_semantic.json`
- `data/processed/semantic/plants_semantic.json`

同时会生成汇总统计：

- `data/processed/semantic/semantic_stats.json`

### 1.2 语义 Embedding 提取（生成 .pt 向量文件）

脚本：`scripts/semantic/run_embedding_extraction.py`

```powershell
cd d:\multimodal_microservice_extraction
python scripts\semantic\run_embedding_extraction.py
```

输出（每个系统至少 1 个）：

- `data/processed/embedding/{system}.pt`
- `data/processed/embedding/{system}_class_embeddings.json`

> 注意：首次运行会加载/下载 HuggingFace 模型，耗时较长。

## 1. 类集合与顺序（通用脚本）

### 1.1 构建类索引（class index）

汇总 callgraph / dependency / semantic / trace 等来源，得到候选类集合及元信息。

脚本：`scripts/multimodal/build_class_index_acmeair.py`

用法示例（Windows / PowerShell）：

```powershell
cd d:\multimodal_microservice_extraction

python scripts/multimodal/build_class_index_acmeair.py --system acmeair
python scripts/multimodal/build_class_index_acmeair.py --system daytrader
python scripts/multimodal/build_class_index_acmeair.py --system jpetstore
python scripts/multimodal/build_class_index_acmeair.py --system plants
```

输出（按 system）：

- `data/processed/fusion/acmeair_classes.json`
- `data/processed/fusion/daytrader_classes.json`
- `data/processed/fusion/jpetstore_classes.json`
- `data/processed/fusion/plants_classes.json`

其中每个类记录是否出现在 callgraph / dependency / semantic 中，例如：

```json
"com.acmeair.web.CustomerREST": {
  "has_callgraph": true,
  "has_dependency": true,
  "has_semantic": true
}
```

### 1.2 构建类顺序（class order）

在 class index 的基础上，选出本次实验使用的类，并确定一个稳定顺序（所有矩阵都按此顺序对齐）。

脚本：`scripts/multimodal/build_class_order_acmeair.py`

```powershell
python scripts/multimodal/build_class_order_acmeair.py --system acmeair
python scripts/multimodal/build_class_order_acmeair.py --system daytrader
python scripts/multimodal/build_class_order_acmeair.py --system jpetstore
python scripts/multimodal/build_class_order_acmeair.py --system plants
```

输出：

- `data/processed/fusion/{system}_class_order.json`

策略（系统内置）：

- 优先选择 `has_semantic == true` 的类；
- 若为空（如 daytrader, plants 当前无语义），回退到 `has_callgraph || has_dependency`。

## 2. 相似度矩阵构建

### 2.1 模态构建（S_sem, S_struct, S_temp）

目前各系统仍使用各自的构建脚本：

- AcmeAir（三模态融合 + DADE 语义 + 纯静态结构）
  - `scripts/multimodal/build_similarity_matrices_acmeair.py`
- Daytrader
  - `scripts/multimodal/build_similarity_matrices_daytrader.py`
- JPetStore
  - `scripts/multimodal/build_similarity_matrices_jpetstore.py`
- PlantsByWebSphere
  - `scripts/multimodal/build_similarity_matrices_plants.py`

以 AcmeAir 为例：

```powershell
# 先构建时序矩阵（通用时序脚本）
python scripts/temporal/build_S_temp_generic.py --system_name acmeair --mode jtl_session

# 再构建多模态相似度矩阵
python scripts/multimodal/build_similarity_matrices_acmeair.py
```

典型输出（AcmeAir）：

- `data/processed/fusion/acmeair_S_sem_dade.npy`  （语义矩阵，DADE 优先）
- `data/processed/temporal/acmeair_S_temp.npy`     （时序矩阵，由 temporal 脚本生成）
- `data/processed/fusion/acmeair_S_struct.npy`     （纯静态结构矩阵）
- `data/processed/fusion/acmeair_S_final.npy`      （语义+结构融合矩阵，用于谱聚类）

### 2.2 可视化说明（仅保留最终时序证据图）

除最终时序证据外，本仓库不再维护/生成其它 heatmap（语义/结构/U 矩阵等）。

- 最终时序证据热力图（可选，用于论文展示 S_temp 的整体形态）：
  - `scripts/temporal/generate_evidence_heatmap.py`
  - 输出：`results/plots/temporal_heatmaps/{system}_S_temp_heatmap.png`

论文数据质量证明仍以表格化诊断结果为准：

- `scripts/multimodal/phase4/diagnose_intra_inter_similarity.py`
- `results/temporal_master_table.csv` / `results/temporal_master_table.md`

## 3. Phase 1 聚类（Louvain，通用）

利用结构+语义融合矩阵做 Phase 1 粗粒度聚类。

脚本：`scripts/multimodal/phase1_clustering_batch.py`

关键参数：

- `SYSTEMS = ['acmeair', 'daytrader', 'jpetstore', 'plants']`  （可按需缩减）
- `ALPHAS = [0.3, 0.5, 0.7]`  （语义/结构权重）
- `TAU = 0.3`  （相似度截断阈值）

运行：

```powershell
python scripts/multimodal/phase1_clustering_batch.py
```

输出示例（以 acmeair 为例）：

- `data/processed/fusion/phase1_acmeair/clusters_phase1_alpha0.5.json`  （cluster → classes）
- `data/processed/fusion/phase1_acmeair/partition_phase1_alpha0.5.json` （class → cluster_id）
- `data/processed/fusion/phase1_acmeair/phase1_stats_alpha0.5.json`     （簇内/簇间相似度、modularity、边密度等）
- `data/processed/fusion/phase1_acmeair/phase1_graph_alpha0.5.png`      （可视化）

## 4. 模态对齐与冲突检查（通用）

脚本：`scripts/multimodal/phase1_summary_check.py`

功能：

- 检查 `S_struct`, `S_sem_dade`, `S_temp` 是否维度一致；
- 打印每个矩阵的稀疏度；
- 粗略计算 structure vs temporal 的 off-diagonal 冲突率。

默认针对 `acmeair`：

```powershell
python scripts/multimodal/phase1_summary_check.py
```

其他系统可以在代码中调用 `main('daytrader')` 等方式运行。

## 5. Spectral Clustering + 业务“盲审”（当前以 AcmeAir 为主）

### 5.1 一轮谱聚类并输出簇成员

脚本：`scripts/multimodal/run_spectral_acmeair.py`

```powershell
python scripts/multimodal/run_spectral_acmeair.py
```

- 输入：`acmeair_S_final.npy` + `acmeair_class_order.json`
- 输出：打印 6 个簇的类名列表，用于业务人员盲审各簇的语义一致性。

（若后续需要支持其它系统，可基于此脚本改成带 `--system`、`--n_clusters` 的通用版本。）

## 6. Ground Truth 与定量评估（当前仅 AcmeAir）

### 6.1 生成 AcmeAir 的 Ground Truth

脚本：`scripts/multimodal/build_acmeair_ground_truth.py`

```powershell
python scripts/multimodal/build_acmeair_ground_truth.py
```

- 输入：`acmeair_class_order.json`
- 输出：`acmeair_ground_truth.json`，使用字符串匹配规则将类映射到 6 个业务域：
  - 0: Customer
  - 1: Booking
  - 2: Flight
  - 3: Auth
  - 4: Main/Web
  - 5: Reporter
  - -1: 噪声（如 Hamcrest），在外部指标计算时会被过滤。

### 6.2 聚类评估（外部 + 内部指标）

脚本：`scripts/multimodal/evaluate_acmeair_clustering.py`

```powershell
python scripts/multimodal/evaluate_acmeair_clustering.py
```

- 聚类：
  - 使用 `acmeair_S_final.npy` + SpectralClustering(n_clusters=6)
- 外部指标（基于 `acmeair_ground_truth.json`）：
  - ARI（Adjusted Rand Index）
  - NMI（Normalized Mutual Information）
  - 每个簇的领域成分组成（Customer / Booking / Flight / Auth / Main/Web / Reporter）
- 内部结构指标（基于 `acmeair_S_struct.npy` + labels）：
  - SM (Structural Modularity)：簇内平均相似度 − 簇间平均相似度（越高越好）
  - ICP (Inter-Connection Proportion)：跨簇链接占比（越低越好）

目前只有 AcmeAir 拥有可对齐的官方微服务拆分，因此 Ground Truth 与 ARI/NMI 评估只针对 AcmeAir。其他系统暂以内部指标和业务人工检查为主。

## 7. 总结：通用脚本 vs AcmeAir 专用脚本

**通用脚本（已支持 4 个系统）：**

- `scripts/multimodal/build_class_index_acmeair.py`  （通过 `--system` 参数）
- `scripts/multimodal/build_class_order_acmeair.py`  （通过 `--system` 参数）
- `scripts/multimodal/plot_similarity_heatmaps.py`
- `scripts/multimodal/phase1_clustering_batch.py`     （`SYSTEMS` 可配置）
- `scripts/multimodal/phase1_summary_check.py`

**当前主要用于 AcmeAir 的专用脚本（可视为模板 / case study）：**

- `scripts/multimodal/build_similarity_matrices_acmeair.py`
- `scripts/multimodal/build_acmeair_ground_truth.py`
- `scripts/multimodal/evaluate_acmeair_clustering.py`
- `scripts/multimodal/run_spectral_acmeair.py`

后续如需为 Daytrader/JPetStore/Plants 引入 Ground Truth 与 ARI/NMI 评估，可基于上述 AcmeAir 脚本扩展为带 `--system` 的通用实现。